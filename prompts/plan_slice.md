# Plan Slice — {{milestone_id}}/{{slice_id}}

Decompose this slice into implementation tasks.

## Project

{{project}}

## Slice Description

{{slice_plan}}

## Context

{{context}}

## Rules

Each task must be completable in a single context window (~50 tool calls). Declare must-haves (Truths, Artifacts, Key Links) for each task. Tasks are executed sequentially within the slice.

## Output Format

Use the same ### T01 — [Title] format with Must-haves and Action sections.
