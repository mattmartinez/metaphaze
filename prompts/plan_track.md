# Plan Track — {{phase_id}}/{{track_id}}

Decompose this track into implementation steps.

## Project

{{project}}

## Track Description

{{track_plan}}

## Context

{{context}}

## Rules

Each step must be completable in a single context window (~50 tool calls). Declare must-haves (Truths, Artifacts, Key Links) for each step. Steps are executed sequentially within the track.

## Output Format

Use the same ### ST01 — [Title] format with Must-haves and Action sections.
